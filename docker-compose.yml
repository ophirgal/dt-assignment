services:
  database:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: appdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      DB_HOST: database
      DB_PORT: 5433
      DB_USER: appuser
      DB_PASSWORD: secret
      DB_NAME: appdb
    depends_on:
      - database

  frontend:
    build: ./frontend
    ports:
      - "5000:80"
    depends_on:
      - backend

volumes:
  pgdata:
